# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for client-side routes (/about, /case-study/..., etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
